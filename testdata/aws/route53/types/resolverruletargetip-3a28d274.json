{
    "properties": {
        "ip": {
            "type": "string",
            "description": "One IP address that you want to forward DNS queries to. You can specify only IPv4 addresses.\n"
        },
        "port": {
            "type": "integer",
            "description": "The port at `ip` that you want to forward DNS queries to. Default value is `53`\n"
        }
    },
    "type": "object",
    "required": [
        "ip"
    ]
}