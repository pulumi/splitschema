{
    "properties": {
        "durationInMinutes": {
            "type": "integer",
            "description": "The time to wait until beginning the next stage. The duration can only be set to 0 if a target is specified.\n"
        },
        "targets": {
            "type": "array",
            "items": {
                "$ref": "#/types/aws:ssmcontacts/PlanStageTarget:PlanStageTarget"
            },
            "description": "One or more configuration blocks for specifying the contacts or contact methods that the escalation plan or engagement plan is engaging. See Target below for more details.\n"
        }
    },
    "type": "object",
    "required": [
        "durationInMinutes"
    ]
}