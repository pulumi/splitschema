{
    "properties": {
        "dnsName": {
            "type": "string",
            "description": "DNS name for the file system (e.g. `fs-12345678.corp.example.com`).\n"
        },
        "ipAddresses": {
            "type": "array",
            "items": {
                "type": "string"
            }
        }
    },
    "type": "object",
    "required": [
        "dnsName",
        "ipAddresses"
    ],
    "language": {
        "nodejs": {
            "requiredInputs": []
        }
    }
}