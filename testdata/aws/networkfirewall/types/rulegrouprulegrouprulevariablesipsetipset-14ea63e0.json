{
    "properties": {
        "definitions": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Set of IP addresses and address ranges, in CIDR notation.\n"
        }
    },
    "type": "object",
    "required": [
        "definitions"
    ]
}