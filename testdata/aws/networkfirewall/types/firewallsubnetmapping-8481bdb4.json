{
    "properties": {
        "ipAddressType": {
            "type": "string",
            "description": "The subnet's IP address type. Valida values: `\"DUALSTACK\"`, `\"IPV4\"`.\n"
        },
        "subnetId": {
            "type": "string",
            "description": "The unique identifier for the subnet.\n"
        }
    },
    "type": "object",
    "required": [
        "subnetId"
    ],
    "language": {
        "nodejs": {
            "requiredOutputs": [
                "ipAddressType",
                "subnetId"
            ]
        }
    }
}