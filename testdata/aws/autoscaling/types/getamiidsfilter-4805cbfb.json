{
    "properties": {
        "name": {
            "type": "string",
            "description": "Name of the DescribeAutoScalingGroup filter. The recommended values are: `tag-key`, `tag-value`, and `tag:\u003ctag name\u003e`\n"
        },
        "values": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Value of the filter.\n"
        }
    },
    "type": "object",
    "required": [
        "name",
        "values"
    ]
}