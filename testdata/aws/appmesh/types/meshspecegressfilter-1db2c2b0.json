{
    "properties": {
        "type": {
            "type": "string",
            "description": "Egress filter type. By default, the type is `DROP_ALL`.\nValid values are `ALLOW_ALL` and `DROP_ALL`.\n"
        }
    },
    "type": "object"
}