{
    "properties": {
        "defaultTargetHostname": {
            "type": "string",
            "description": "Default target host name to write to. Valid values: `ENABLED`, `DISABLED`.\n"
        }
    },
    "type": "object",
    "required": [
        "defaultTargetHostname"
    ]
}