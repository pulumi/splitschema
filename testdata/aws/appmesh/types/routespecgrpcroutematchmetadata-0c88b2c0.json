{
    "properties": {
        "invert": {
            "type": "boolean",
            "description": "If `true`, the match is on the opposite of the `match` criteria. Default is `false`.\n"
        },
        "match": {
            "$ref": "#/types/aws:appmesh/RouteSpecGrpcRouteMatchMetadataMatch:RouteSpecGrpcRouteMatchMetadataMatch",
            "description": "Data to match from the request.\n"
        },
        "name": {
            "type": "string",
            "description": "Name of the route. Must be between 1 and 50 characters in length.\n"
        }
    },
    "type": "object",
    "required": [
        "name"
    ]
}