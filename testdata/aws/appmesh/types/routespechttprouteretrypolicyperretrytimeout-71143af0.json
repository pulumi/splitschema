{
    "properties": {
        "unit": {
            "type": "string",
            "description": "Retry unit. Valid values: `ms`, `s`.\n"
        },
        "value": {
            "type": "integer",
            "description": "Retry value.\n"
        }
    },
    "type": "object",
    "required": [
        "unit",
        "value"
    ]
}