{
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the property that is included in this view. Valid values: `tags`.\n"
        }
    },
    "type": "object",
    "required": [
        "name"
    ]
}