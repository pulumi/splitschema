{
    "properties": {
        "frequency": {
            "type": "string",
            "description": "Specifies how frequently inventory results are produced. Valid values: `Daily`, `Weekly`.\n"
        }
    },
    "type": "object",
    "required": [
        "frequency"
    ]
}