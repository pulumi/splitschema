{
    "properties": {
        "status": {
            "type": "string",
            "description": "Whether the existing objects should be replicated. Either `\"Enabled\"` or `\"Disabled\"`.\n"
        }
    },
    "type": "object",
    "required": [
        "status"
    ]
}