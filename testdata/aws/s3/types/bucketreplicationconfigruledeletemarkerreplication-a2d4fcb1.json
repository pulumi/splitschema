{
    "properties": {
        "status": {
            "type": "string",
            "description": "Whether delete markers should be replicated. Either `\"Enabled\"` or `\"Disabled\"`.\n"
        }
    },
    "type": "object",
    "required": [
        "status"
    ]
}