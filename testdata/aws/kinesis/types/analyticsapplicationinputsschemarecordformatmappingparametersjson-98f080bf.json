{
    "properties": {
        "recordRowPath": {
            "type": "string",
            "description": "Path to the top-level parent that contains the records.\n"
        }
    },
    "type": "object",
    "required": [
        "recordRowPath"
    ]
}