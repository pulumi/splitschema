{
    "properties": {
        "streamMode": {
            "type": "string",
            "description": "Capacity mode of the stream. Either `ON_DEMAND` or `PROVISIONED`.\n"
        }
    },
    "type": "object",
    "required": [
        "streamMode"
    ],
    "language": {
        "nodejs": {
            "requiredInputs": []
        }
    }
}