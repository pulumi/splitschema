{
    "properties": {
        "streamMode": {
            "type": "string",
            "description": "Specifies the capacity mode of the stream. Must be either `PROVISIONED` or `ON_DEMAND`.\n"
        }
    },
    "type": "object",
    "required": [
        "streamMode"
    ]
}