{
    "properties": {
        "enabled": {
            "type": "boolean",
            "description": "Whether or not serial console access is enabled. Valid values are `true` or `false`. Defaults to `true`.\n"
        }
    },
    "inputProperties": {
        "enabled": {
            "type": "boolean",
            "description": "Whether or not serial console access is enabled. Valid values are `true` or `false`. Defaults to `true`.\n"
        }
    },
    "stateInputs": {
        "description": "Input properties used for looking up and filtering SerialConsoleAccess resources.\n",
        "properties": {
            "enabled": {
                "type": "boolean",
                "description": "Whether or not serial console access is enabled. Valid values are `true` or `false`. Defaults to `true`.\n"
            }
        },
        "type": "object"
    }
}