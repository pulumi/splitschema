{
    "properties": {
        "subnetId": {
            "type": "string",
            "description": "The ID of the subnet to be associated with the VPC endpoint.\n"
        },
        "vpcEndpointId": {
            "type": "string",
            "description": "The ID of the VPC endpoint with which the subnet will be associated.\n"
        }
    },
    "required": [
        "subnetId",
        "vpcEndpointId"
    ],
    "inputProperties": {
        "subnetId": {
            "type": "string",
            "description": "The ID of the subnet to be associated with the VPC endpoint.\n",
            "willReplaceOnChanges": true
        },
        "vpcEndpointId": {
            "type": "string",
            "description": "The ID of the VPC endpoint with which the subnet will be associated.\n",
            "willReplaceOnChanges": true
        }
    },
    "requiredInputs": [
        "subnetId",
        "vpcEndpointId"
    ],
    "stateInputs": {
        "description": "Input properties used for looking up and filtering VpcEndpointSubnetAssociation resources.\n",
        "properties": {
            "subnetId": {
                "type": "string",
                "description": "The ID of the subnet to be associated with the VPC endpoint.\n",
                "willReplaceOnChanges": true
            },
            "vpcEndpointId": {
                "type": "string",
                "description": "The ID of the VPC endpoint with which the subnet will be associated.\n",
                "willReplaceOnChanges": true
            }
        },
        "type": "object"
    }
}