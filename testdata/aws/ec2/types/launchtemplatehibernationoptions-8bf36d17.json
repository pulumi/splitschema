{
    "properties": {
        "configured": {
            "type": "boolean",
            "description": "If set to `true`, the launched EC2 instance will hibernation enabled.\n"
        }
    },
    "type": "object",
    "required": [
        "configured"
    ]
}