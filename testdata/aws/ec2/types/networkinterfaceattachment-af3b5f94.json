{
    "properties": {
        "attachmentId": {
            "type": "string"
        },
        "deviceIndex": {
            "type": "integer",
            "description": "Integer to define the devices index.\n"
        },
        "instance": {
            "type": "string",
            "description": "ID of the instance to attach to.\n"
        }
    },
    "type": "object",
    "required": [
        "deviceIndex",
        "instance"
    ],
    "language": {
        "nodejs": {
            "requiredOutputs": [
                "attachmentId",
                "deviceIndex",
                "instance"
            ]
        }
    }
}