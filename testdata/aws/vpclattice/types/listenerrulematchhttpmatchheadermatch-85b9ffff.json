{
    "properties": {
        "caseSensitive": {
            "type": "boolean",
            "description": "Indicates whether the match is case sensitive. Defaults to false.\n"
        },
        "match": {
            "$ref": "#/types/aws:vpclattice/ListenerRuleMatchHttpMatchHeaderMatchMatch:ListenerRuleMatchHttpMatchHeaderMatchMatch",
            "description": "The header match type.\n"
        },
        "name": {
            "type": "string",
            "description": "The name of the header.\n"
        }
    },
    "type": "object",
    "required": [
        "match",
        "name"
    ]
}