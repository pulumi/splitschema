{
    "properties": {
        "behavior": {
            "type": "string",
            "description": "The cache behavior of the distribution. Valid values: `cache` and `dont-cache`.\n"
        }
    },
    "type": "object",
    "required": [
        "behavior"
    ]
}