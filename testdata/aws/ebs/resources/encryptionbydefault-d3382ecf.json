{
    "properties": {
        "enabled": {
            "type": "boolean",
            "description": "Whether or not default EBS encryption is enabled. Valid values are `true` or `false`. Defaults to `true`.\n"
        }
    },
    "inputProperties": {
        "enabled": {
            "type": "boolean",
            "description": "Whether or not default EBS encryption is enabled. Valid values are `true` or `false`. Defaults to `true`.\n"
        }
    },
    "stateInputs": {
        "description": "Input properties used for looking up and filtering EncryptionByDefault resources.\n",
        "properties": {
            "enabled": {
                "type": "boolean",
                "description": "Whether or not default EBS encryption is enabled. Valid values are `true` or `false`. Defaults to `true`.\n"
            }
        },
        "type": "object"
    }
}