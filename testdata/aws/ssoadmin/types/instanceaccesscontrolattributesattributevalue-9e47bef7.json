{
    "properties": {
        "sources": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "The identity source to use when mapping a specified attribute to AWS SSO.\n"
        }
    },
    "type": "object",
    "required": [
        "sources"
    ]
}