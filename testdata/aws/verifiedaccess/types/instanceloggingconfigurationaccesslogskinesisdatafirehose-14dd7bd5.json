{
    "properties": {
        "deliveryStream": {
            "type": "string",
            "description": "The name of the delivery stream.\n"
        },
        "enabled": {
            "type": "boolean",
            "description": "Indicates whether logging is enabled.\n"
        }
    },
    "type": "object",
    "required": [
        "enabled"
    ]
}