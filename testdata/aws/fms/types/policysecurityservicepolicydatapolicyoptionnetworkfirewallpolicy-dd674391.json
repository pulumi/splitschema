{
    "properties": {
        "firewallDeploymentModel": {
            "type": "string",
            "description": "Defines the deployment model to use for the third-party firewall policy. Valid values are `CENTRALIZED` and `DISTRIBUTED`.\n"
        }
    },
    "type": "object"
}