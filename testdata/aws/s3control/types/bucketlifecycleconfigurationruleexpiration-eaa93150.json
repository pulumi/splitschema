{
    "properties": {
        "date": {
            "type": "string",
            "description": "Date the object is to be deleted. Should be in `YYYY-MM-DD` date format, e.g., `2020-09-30`.\n"
        },
        "days": {
            "type": "integer",
            "description": "Number of days before the object is to be deleted.\n"
        },
        "expiredObjectDeleteMarker": {
            "type": "boolean",
            "description": "Enable to remove a delete marker with no noncurrent versions. Cannot be specified with `date` or `days`.\n"
        }
    },
    "type": "object"
}