{
            "properties": {
                "frameOption": {
                    "type": "string",
                    "description": "The value of the `X-Frame-Options` HTTP response header. Valid values: `DENY` | `SAMEORIGIN`\n"
                },
                "override": {
                    "type": "boolean",
                    "description": "Whether CloudFront overrides the `X-Frame-Options` HTTP response header received from the origin with the one specified in this response headers policy.\n"
                }
            },
            "type": "object",
            "required": [
                "frameOption",
                "override"
            ]
        }