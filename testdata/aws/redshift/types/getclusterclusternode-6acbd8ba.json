{
    "properties": {
        "nodeRole": {
            "type": "string",
            "description": "Whether the node is a leader node or a compute node\n"
        },
        "privateIpAddress": {
            "type": "string",
            "description": "Private IP address of a node within a cluster\n"
        },
        "publicIpAddress": {
            "type": "string",
            "description": "Public IP address of a node within a cluster\n"
        }
    },
    "type": "object",
    "required": [
        "nodeRole",
        "privateIpAddress",
        "publicIpAddress"
    ],
    "language": {
        "nodejs": {
            "requiredInputs": []
        }
    }
}