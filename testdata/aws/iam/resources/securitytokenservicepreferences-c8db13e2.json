{
    "properties": {
        "globalEndpointTokenVersion": {
            "type": "string",
            "description": "The version of the STS global endpoint token. Valid values: `v1Token`, `v2Token`.\n"
        }
    },
    "required": [
        "globalEndpointTokenVersion"
    ],
    "inputProperties": {
        "globalEndpointTokenVersion": {
            "type": "string",
            "description": "The version of the STS global endpoint token. Valid values: `v1Token`, `v2Token`.\n"
        }
    },
    "requiredInputs": [
        "globalEndpointTokenVersion"
    ],
    "stateInputs": {
        "description": "Input properties used for looking up and filtering SecurityTokenServicePreferences resources.\n",
        "properties": {
            "globalEndpointTokenVersion": {
                "type": "string",
                "description": "The version of the STS global endpoint token. Valid values: `v1Token`, `v2Token`.\n"
            }
        },
        "type": "object"
    }
}