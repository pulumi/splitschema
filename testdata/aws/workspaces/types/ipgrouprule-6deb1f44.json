{
    "properties": {
        "description": {
            "type": "string",
            "description": "The description of the IP group.\n"
        },
        "source": {
            "type": "string",
            "description": "The IP address range, in CIDR notation, e.g., `10.0.0.0/16`\n"
        }
    },
    "type": "object",
    "required": [
        "source"
    ]
}