{
    "properties": {
        "applyOn": {
            "type": "string",
            "description": "Conditions where snap start is enabled. Valid values are `PublishedVersions`.\n"
        },
        "optimizationStatus": {
            "type": "string"
        }
    },
    "type": "object",
    "required": [
        "applyOn"
    ],
    "language": {
        "nodejs": {
            "requiredOutputs": [
                "applyOn",
                "optimizationStatus"
            ]
        }
    }
}