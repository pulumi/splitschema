{
            "properties": {
                "type": {
                    "type": "string",
                    "description": "The rule type, either `REGULAR`, `RATE_BASED`, or `GROUP`. Defaults to `REGULAR`.\n"
                }
            },
            "type": "object",
            "required": [
                "type"
            ]
        }