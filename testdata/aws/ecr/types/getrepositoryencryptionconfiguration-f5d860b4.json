{
    "properties": {
        "encryptionType": {
            "type": "string",
            "description": "Encryption type to use for the repository, either `AES256` or `KMS`.\n"
        },
        "kmsKey": {
            "type": "string",
            "description": "If `encryption_type` is `KMS`, the ARN of the KMS key used.\n"
        }
    },
    "type": "object",
    "required": [
        "encryptionType",
        "kmsKey"
    ],
    "language": {
        "nodejs": {
            "requiredInputs": []
        }
    }
}