{
    "properties": {
        "languageCode": {
            "type": "string",
            "description": "Selects a specific three-letter language code from within an audio source.\n"
        },
        "name": {
            "type": "string",
            "description": "Name of the Channel.\n\nThe following arguments are optional:\n"
        },
        "selectorSettings": {
            "$ref": "#/types/aws:medialive/ChannelInputAttachmentInputSettingsCaptionSelectorSelectorSettings:ChannelInputAttachmentInputSettingsCaptionSelectorSelectorSettings",
            "description": "The audio selector settings. See Audio Selector Settings for more details.\n"
        }
    },
    "type": "object",
    "required": [
        "name"
    ]
}