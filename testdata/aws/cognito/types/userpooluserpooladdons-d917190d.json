{
    "properties": {
        "advancedSecurityMode": {
            "type": "string",
            "description": "Mode for advanced security, must be one of `OFF`, `AUDIT` or `ENFORCED`.\n"
        }
    },
    "type": "object",
    "required": [
        "advancedSecurityMode"
    ]
}